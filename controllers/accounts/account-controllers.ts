import { NextFunction, Request, Response } from "express";
import expressAsyncHandler from "express-async-handler";

export const getAllAccountsAsync = expressAsyncHandler(async (request: Request, response: Response, next: NextFunction) => {

})

export const getAccountByIdAsync = expressAsyncHandler(async (request: Request, response: Response, next: NextFunction) => {
    
})